{
  "mcpServers": {
    "whatsapp": {
      "command": "bash",
      "args": ["${CLAUDE_PLUGIN_ROOT}/start.sh"],
      "env": {
        "PLUGIN_ROOT": "${CLAUDE_PLUGIN_ROOT}",
        "PLUGIN_DATA": "${CLAUDE_PLUGIN_DATA}"
      }
    }
  }
}
