# Evolution API connection
EVOLUTION_API_URL=http://localhost:8080
EVOLUTION_API_KEY=your-api-key-here
EVOLUTION_INSTANCE=your-instance-name

# Your WhatsApp phone number (digits only, with country code, no + or spaces)
# Example: 15551234567  or  447700900000
ALLOWED_NUMBER=15551234567

# Optional: newer WhatsApp multi-device accounts use @lid JIDs instead of phone numbers
# for inbound messages. If your messages are being blocked, check the Claude Code terminal
# for a line like:
#   [whatsapp] Blocked — add ALLOWED_JID=76875653206151 to your .env and restart
# Copy that number here:
ALLOWED_JID=

# Port the local webhook listener binds to (must match your Evolution API webhook URL)
# Evolution API webhook URL should be: http://host.docker.internal:<WEBHOOK_PORT>/webhook
# (use host.docker.internal if Evolution API runs in Docker; localhost otherwise)
WEBHOOK_PORT=3456
